---
# End tasks

- name: reload toadfarm
  command: "PATH=$PATH:/usr/local/bin /home/stein/git/perlbrew-cron/perlbrew-cron.sh script/toadfarm.pl reload"
  become: yes
  become_user: www
  tags: [git,perl,perlbrew]
  args:
    chdir: /home/stein/git/webserver/
