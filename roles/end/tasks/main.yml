---
# End tasks

- name: reload toadfarm
  command: "/bin/env /home/stein/git/perlbrew-cron/perlbrew-cron.sh script/toadfarm.pl reload"
  become: yes
  become_user: www
  tags: [git,perl,perlbrew]
  args:
    chdir: /home/stein/git/webserver/
# PERLBREW_ROOT=/opt/perl5 PERLBREW_HOME=/home/www/.perlbrew PATH=$PATH:/usr/local/bin